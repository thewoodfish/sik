{
  "name": "sik",
  "private": true,
  "version": "0.0.0",
  "description": "Solana Identity Kit — open identity standard built on SNS",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm --filter @sik-sdk/dashboard dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "devDependencies": {
    "next": "^14.2.3",
    "typescript": "^5.4.5"
  }
}
